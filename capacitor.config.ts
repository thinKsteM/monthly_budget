import type { CapacitorConfig } from '@capacitor/core';

const config: CapacitorConfig = {
  appId: 'com.example.monthlybudget',
  appName: '月次収支管理',
  webDir: 'www',
  ios: {
    backgroundColor: '#1a1b26',
  }
};

export default config;
